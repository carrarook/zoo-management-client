{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch Chrome against localhost",
        "type": "msedge",
        "request": "launch",
        "url": "http://localhost:3000",  // URL do seu React App
        "webRoot": "${workspaceFolder}/src"
      }
    ]
  }
  